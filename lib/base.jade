extends lib.tpl.jade

block append vars
  - var page_id = lib[2]
  - titles.push(page_id.name)

block toc
    +libtoc({pages: page_id.sub, depth: Infinity})

block append main
  include includes/_content-base.jade
