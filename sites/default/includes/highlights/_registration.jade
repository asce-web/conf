h1#registration Registration
+passList([
  site.getCurrentConference().getPass('Standard Pass')
, site.getCurrentConference().getPass('Student Pass')
, site.getCurrentConference().getPass('Younger Member')
], site.getCurrentConference())
+highlightButtons([{text:'Registration Details', href:'registration.html'}])
